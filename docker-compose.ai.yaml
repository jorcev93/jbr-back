services:
  ollama:
    image: ollama/ollama:latest
    container_name: jbre-ollama
    restart: always
    ports:
      - "11434:11434"
    volumes:
      - ./data/ollama:/root/.ollama
    environment:
      - OLLAMA_HOST=0.0.0.0
